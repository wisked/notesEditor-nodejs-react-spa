import React, { PropTypes } from 'react'

const Note = React.createClass({
    render () {
        return (
            <h1>
                Note
            </h1>
        )
    }
})

export default Note;
